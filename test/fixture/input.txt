/**
 * addOnloadHook
 */

function addOnloadHook() {
}

call(function addOnloadHook() {
	// ..
})

addOnloadHook(function addOnloadHook() {});
addOnloadHook(function () {});
 addOnloadHook(funcRef );
  addOnloadHook(function() {
    // ..
  })
  // addOnloadHook(function() {})

DatRoot.addOnloadHook();

/**
 * escapeQuotesHTML
 */

pg.escapeQuotesHTML("foo")
	+ pg.escapeQuotesHTML(bar);
escapeQuotesHTML("foo")
	+ escapeQuotesHTML(bar);
window.escapeQuotesHTML("foo")
	+ window.escapeQuotesHTML(bar);

/**
 * $j
 */

$j ( "#test" );
$j( function () {} );
$j.ajax();
var $jay = $('.jay');

/**
 * jquery.mwExtension
 */

$.trimLeft( 'foo' );
$.trimLeft( 'foo'.replace('foo', 'bar') );

$.trimRight( 'foo' );
$.trimRight( 'foo'.replace('foo', 'bar') );

/**
 * mw.Map
 */

	if( !mw.config.values.wgArticleId || $(frm.wpSection).val() == 'new' ) {
		return
	}

	if (nstab && ((mw.config.values.wgUserLanguage=='cdo') || (mw.config.values.wgUserLanguage=='cdo'))) {

var conf = window.mw ? mw.config.values : window;

{
'token' : mw.user.tokens.values.editToken,
'rows' : mw.user.options.values.rows
}

$.each(mw.user.options.values, function (key) {
});
Object.keys(mw.user.options.values)
			.filter()

/**
 * mediawiki.user, mediawiki.util
 */

mw.util.addPortletLink( 'p-tb', mw.util.wikiGetlink( 'Special:PrefixIndex' ), 'Unterseiten', 't-subpages' );
log('<strong>Welcome, ' + mw.user.name(), mw.user.anonymous());

/**
 * importScriptURI, importStylesheetURI, maxage, normalise action=raw
 */

importScriptURI( '//test.wikipedia.org/w/index.php?title=Example&action=raw&ctype=text/javascript&maxage=0&smaxage=0');

importStylesheetURI("http://test.wikipedia.org/w/index.php?title=Example&action=raw&ctype=text/css&maxage=0&smaxage=0");

importStylesheetURI('//commons.wikimedia.org/w/index.php?action=raw&ctype=text/css&title=MediaWiki:Gadget-Cat-a-lot.css');

/* css:
@import url('//en.wikipedia.org/w/index.php?title=MediaWiki:Gadget-navpop.css&action=raw&ctype=text/css&smaxage=21600&maxage=86400');

@import url('//en.wikipedia.org/w/index.php?title=MediaWiki:Gadget-navpop.css&action=raw&ctype=text/css&bcache=1&smaxage=21600&maxage=86400');
*/

try {
	$.parseJSON( optionsText );
} catch ( e ) {
	// Literal plain text
	mw.util.jsMessage("Could not parse twinkleoptions.js");
	mw.util.jsMessage( 'Could not parse twinkleoptions.js', opt );
}

// Literal HTML (must call $.parseHTML before mw.notify)
mw.util.jsMessage("<div>Hi</div>");
mw.util.jsMessage( '<div>Hi</div>' );

// Variable (unsure whether text or HTML)
mw.util.jsMessage( data.watch.message, 'watch' );

/**
 * Toolserver url
 */

load('https://toolserver.org/~kolossos/openlayers/kml-on-ol.php');
load( 'http://toolserver.org/~kolossos/openlayers/kml-on-ol.php' );
load( "//toolserver.org/~kolossos/openlayers/kml-on-ol.php");
var sLink = 'http://tools.wikimedia.de/~daniel/WikiSense/Contributors.php?';
  "http://tools.wikimedia.de/~luxo/contributions/contributions.php?user=" + encodeURI(user);
add('p-tb', 'http://tools.wikimedia.de/~magnus/fist.php?doit=1', 't-fist');

/*
 * bits.wikimedia.org, static-current, and skins-1.5
 */

var x = "//bits.wikimedia.org/skins/Vector/images/watch-icon.png";
url(//bits.wikimedia.org/static-current/skins/MonoBook/bullet.gif);
url(//bits.wikimedia.org/static/current/skins/MonoBook/bullet.gif);

var s=document.createElement('script');
s.setAttribute('src',"//bits.wikimedia.org/he.wikipedia.org/load.php?modules=jquery&only=scripts");
s.setAttribute('src',"http://bits.wikimedia.org/he.wikipedia.org/load.php?modules=jquery&only=scripts");

url(//bits.wikimedia.org/static-1.22wmf1/skins/vector/images/search-ltr.png?303-4);
url(//bits.wikimedia.org/static-1.25wmf1/skins/Vector/images/search-ltr.png);

url(/w/skins/Vector/images/search-ltr.png?303-4);
url(https://example.org/logo.png?303-4);

url(//bits.wikimedia.org/skins-1.5/vector/images/watch-icon.png);
url(//bits.wikimedia.org/skins-1.5/Vector/images/watch-icon.png);

var x = "https://en.wikipedia.org/skins-1.5/monobook/headbg.jpg";
url(/skins-1.5/monobook/headbg.jpg);
url("/skins-1.5/monobook/headbg.jpg");

// This /skins-1.5/common/edit.js
// and /skins-1.5/commons/wikibits.js
// and /w/skins-1.5/monobook/main.css.
function popupTableau() {
 var popup = window.open('','name','height=200,width=250');
 popup.document.write('<html><head><title>Example</title>');
 popup.document.write('<script type="text\/javascript" src="\/skins-1.5\/common\/wikibits.js"><!-- wikibits js --><\/script>');
 popup.document.write('<script src="\/skins-1.5\/common\/wikibits.js"><!-- wikibits js --><\/script>');
 popup.document.write('<script src="\/skins-1.5\/common\/wikibits.js"><\/script>');
 popup.document.write("<script src='\/w\/skins-1.5\/common\/wikibits.js'><\/script>");
 popup.document.write('<script src="\/skins-1.5\/unrelated.js"><\/script>');
 popup.document.write('</head><body>');
 popup.document.write('<style type=\"text\/css\" media=\"screen,projection\">/*<![CDATA[*/ @import "\/skins-1.5\/monobook\/main.css?5\";@import \"/skins-1.5/monobook/rtl.css\"; /*]]>*/<\/style>');
}

/**
 * Wdsearch
 */
mw.loader.load( '//sv.wikipedia.org/w/index.php?title=MediaWiki:Gadget-Wdsearch.js&action=raw&ctype=text/javascript' );

/**
 * Clean up
 */
mw.config.get('wgPageName').charAt(0).toUpperCase();
addPortletLink('p-cactions',wgServer + wgScript + '?title=' + encodeURIComponent(wgPageName) + '&uselang=en', 'en');
addPortletLink('p-cactions', wgServer + wgScript + '?title=' + encodeURIComponent(wgPageName) + '&action=purge', 'purge', 'ca-purge', 'Purge the internal cache for this page'); });

/**
 * wgIsMainPage
 */
if(mw.config.get('wgTitle') == 'Main Page' && mw.config.get('wgNamespaceNumber') == 0) {
	a;
}
if(wgTitle == 'Main Page' && wgNamespaceNumber == 0) {
	b;
}
if (mw.config.get('wgPageName') == 'Main_Page' || mw.config.get('wgPageName') == 'Tartışma:Ana_Sayfa') {
}

/**
 * wgScriptPaath
 */
xhr.open('POST', wgServer + wgScriptPath + '/api' + wgScriptExtension + '?format=xml&action=' + action, true);
xhr.open('/api' + wgScriptExtension + "?format=xml&action=");
xhr.open("/api" + wgScriptExtension + "?format=xml&action=");

this.url = wgScriptPath + "/api" + wgScriptExtension
this.url = wgScriptPath + "/api" + wgScriptExtension;
set({
 api_uri: window.location.protocol + wgServer + wgScriptPath + "/api" + wgScriptExtension,
 api_uri: window.location.protocol + wgServer + wgScriptPath + '/api' + wgScriptExtension,
 x: 1
});

/**
 * jQuery
 */
.size()

obj.removeAttr('disabled');
attr('disabled', true)
$(form).find(':submit').removeAttr('disabled');

/**
 * Obsolete modules: es5-shim, json, dom-level2-shim
 */

{
	if ( !$.jStorage ) dependencies.push( 'jquery.jStorage' );
	if ( !Object.keys ) dependencies.push( 'es5-shim' );
	if ( !Object.keys ) dependencies.push( 'json' );
}
// ...
    mw.loader.using( ['es5-shim', 'mediawiki.util', 'x'], function() { $(function(){
    mw.loader.using( ['mediawiki.util', 'es5-shim', 'x'], function() { $(function(){
    mw.loader.using( ['mediawiki.util', 'x', 'es5-shim'], function() { $(function(){
    mw.loader.using( ["json", "x","dom-level2-shim", "es5-shim"], func)
// ...

/**
 * Renamed module: WikiEditor
 */

mw.loader.using( [ "x', 'ext.wikiEditor.toolbar'] );
mw.loader.using( [ "ext.wikiEditor.toolbar" ] );
mw.loader.load( "ext.wikiEditor.toolbar");

/**
 * Renamed module: mediawiki.api
 */

mw.loader.using( [ "x', 'mediawiki.api.edit'] );
mw.loader.using( [ "mediawiki.api.edit" ] );
mw.loader.load( "mediawiki.api.edit");
