$j ( "#test" );
not$j( "#test" );
$j( function () {} );
$j.ajax();
var $jay = $('.jay');
$jay.ajax();
if (typeof $j != 'undefined' && typeof $j.fn.wikiEditor != 'undefined') {}
var hasjQuery = typeof $j != 'undefined';
var hasJay = typeof $j != 'undefined';

.size()

obj.removeAttr('disabled');
obj .removeAttr ( "autofocus" );
attr('disabled', true)
$(form).find(':submit').removeAttr('disabled');

$(this).nextUntil("h2").andSelf().wrapAll('<div class="mw-h2section"></div>');

$.parseJSON(x);
$j.parseJSON(y.foo());
jQuery.parseJSON( y.foo.json() );
        var antwort = $.parseJSON ($.ajax (
                {
                    url: mw.util.wikiScript("api"),
                    data: params,
                    async: false,
                    dataType: "json"
                }).responseText);
